# 歌词分节翻页 + 手动/自动切换 + 曲谱歌词模式（实现与实机验证）

- 日期：2026-09-19
- 工具链：Cline + DeepSeek API
- 涉及模块：Windows 歌词显示 / 状态持久化 / 曲谱数据（hymn_score）/ 实机 UI 验证

## 任务 1：按节翻页歌词 + 手动/自动 + 曲谱+歌词模式（实现）

### 提问

歌词按「一节一页」分页显示（正歌+副歌为一页），提供上一页/下一页/页码导航与「手动/自动」切换（自动按音频时长等分推进）；新增「曲谱」显示模式，简谱谱行与歌词逐列对齐、副歌谱每页重复、任何字号下不换行。

### 解决思路

- 分页语义：`LyricPage` = 一节正歌 + 副歌（如有）；页数 = 正歌节数，副歌不增页。
- 自动翻页：播放器时长优先，为 0/不可用时回退 DB `audio_durations`；进度→页索引换算并钳位末页。
- 曲谱对齐：**不在运行时重做歌词-音符对齐**，直接采用库内第 1 节已存的 `hymn_score_char.note_index` 列位模板，各节歌词按字序落列（一谱多词天然对齐）。
- 不换行硬约束：字号上限 = 可用宽 ÷ 页内最宽谱行（全角 2 / 半角 1 度量）。
- 持久化：`lyricAutoMode` 走 `AppState`/`AppStateService` 既有串行写队列。

### 最终结果

- **完成结论**：全部实现完成；`test/lyric_page_score_test.dart` 39/39 通过；`flutter analyze` 无告警；Windows release 构建成功。
- **关键改动**：`models/hymn.dart`（chorus/audio_durations 解析）、`models/hymn_score.dart`（新增，谱模型+code_seq 还原）、`widgets/hymn_display.dart`（分页/导航/手动自动/模式切换）、`widgets/score_lyric_view.dart`（新增，曲谱歌词渲染）、`services/sqlite_repository.dart`（谱表检查/loadScorePages/码位缓存/副歌谱探测）、`services/app_state_service.dart` + `screens/home_screen.dart`（lyricAutoMode 持久化接线）。
- **验证方式**：单元测试 39 用例 + release 构建 + 实机截图点击验证（见任务 2）。
- **遗留问题**：无（功能层面）。

---

## 任务 2：实机验证（Win32 合成点击 + 截图比对）

### 提问

在 release 真机上验证：手动翻页（含首页/末页禁用）、手动/自动切换的可见变化与重启持久化。

### 解决思路

- 用 PowerShell + Win32 `SetCursorPos/mouse_event` 合成点击，`CopyFromScreen` 截图，Python/PIL 像素扫描定位控件、图像差分判定点击是否生效。
- 关键坑：截图（.NET VirtualScreen）与 `SetCursorPos`（pwsh 为 DPI-unaware，DC 报 96DPI/2048 宽）处于**同一逻辑坐标空间，1:1 直用**；早期按 1.2 比例换算导致点击偏移误判「未生效」。
- 判定点击生效最可靠的通道是**应用日志**（`logs/` 的 `[ACTION]` 条目），优于像素差分。

### 最终结果

- **完成结论**：手动翻页全链路实机验证通过；手动/自动切换与持久化经 `state.json` + 日志证实生效。
- **验证细节**：
  - 翻页：343 首 3 页，「下一页」推进至「第 3 / 3 节」时 `>` 置灰禁用、`<` 可回退——正确。
  - 模式按钮：日志确认 lyrics/scoreLyric/numbered/staff 四种切换均响应。
  - 手动/自动：`state.json` 落盘 `lyricAutoMode: false`（默认 true），证明切换生效且持久化写通道正常。
  - 音频自然播完自动下一首（343→339）联动正常。
- **遇到的困难**：① 截图坐标空间误判（以为需 ÷1.2 换算），实际 1:1；② 小按钮点击坐标需像素扫描取真实边界（按钮行 y≈159–188），凭视觉估算多次落空；③ 用户同时在手动操作应用，合成点击与真实操作交织，最终以应用日志为准裁定。
- **遗留问题**：重启后 `lyricAutoMode` UI 恢复的目检未单独做（落盘已证实，恢复逻辑有单测覆盖）；验证期截图工具链已按一次性产物清理，未入库。

---

## 会话总结

- 本次共处理 2 个任务（实现 + 实机验证）。
- 关键收获：
  1. **Win32 合成点击验证 Flutter 桌面应用**：截图与 SetCursorPos 在 DPI-unaware 进程下同为逻辑坐标空间（1:1）；控件定位必须像素扫描取边界，点击生效判定以应用日志最可靠。
  2. 曲谱-歌词对齐采用「第 1 节存储列位模板」复用策略，避免运行时重对齐的复杂度与不确定性。
- 遗留事项：
  - 提交前需用户确认 `data/`（tjc_hymn.db / checksums.json / api_cache / lyrics_progress.json）是否随代码一起入库。
  - 临时验证脚本与截图已全部清理（含曾嵌 API key 的 `_tmp_vision.py`，未入 git）。
  - release 构建验证需应用关闭后重跑一次（构建产物被运行中的 exe 占用时无法覆盖）。

---

## 任务 3：简谱曲谱视觉校准·前 50 首（2026-09-19 深夜）

- **提问**：用户报告「曲谱歌词，标点符号有问题」→ 升级为「数据库中的简谱曲谱内容与实际简谱图片差距太大，选前 50 首做图片 OCR 识别、校准数据库错误项，评估 token 消耗。强调：简谱曲谱对应图片的女高部分（第一行）」。
- **解决思路**：
  1. 先诊断：拉 DB 谱表结构 + 渲染 hymn 1 的 PDF/PNG 对照，发现 5 条"主旋律"行中 L8=男低、L15=男高（声部标错），真女高 L9 漏标，且高音1̇ 码位 `4e5e` 未映射显示为 `?`。
  2. OCR 全谱转录试点：整页 qwen-vl-max 会串声部；按 PDF 几何裁剪女高谱带 + 堆叠后，`qwen3-vl-plus` 可转录但对该艺术字体 **2/3、高音点误读率高**（人工目视裁定：DB 对、OCR 错）。
  3. 关键洞察：**PDF 文本层与图片同源、字符级可信**，错误只在声部选择与码位映射 → 方案改为「几何重建 + 字形识别补映射 + OCR 仅做结构验证」。
  4. 未知码位（4e5e/4e5f/5d29/5d2e/531c 等 14 个）从 PDF 渲染单字形，千问视觉识别 + 人工看字形对照表定案：高音1̇/低音2̣/升/降/异体3。
  5. 系统检测：歌词字符 y 聚行→聚组（自适应阈值）= 系统；每系统歌词上方连续谱链最高行 = 女高；旧 chars 与新女高按**歌词文本内容配对**（防 y 序错乱），元素数不同者按 PDF 字符 x 重对位。
- **最终结果**：
  - 完成：前 50 首 191 行女高迁移落库（备份 `tjc_hymn.bak-20260919.db`）；`hymn_codepoint_map` 新增 5 码位；notes 重解码清零 `?`/`@`；app `_symDisplay` 支持低音点 U+0323、seed 同步；测试 41/41、analyze 全绿、release 构建通过。
  - 改动：`data/tjc_hymn.db`、`hymn_app/lib/models/hymn_score.dart`、`hymn_app/lib/widgets/score_lyric_view.dart`、`hymn_app/test/lyric_page_score_test.dart`、`docs/Windows/UI_CONFIRMATION.md`（5.12 节）、工具脚本 `tools/_rebuild_dryrun.py`/`_rebuild_apply.py`/`_crop_bands.py`/`_ocr_score.py`/`_verify_ocr.py`（未入库，供扩量复用）。
  - 验证：DB notes 与女高谱带裁剪图逐行人工比对（hymn 1/3 完全一致）；50 首 OCR 复核结构性差异 = 0。
  - **Token 消耗（DashScope 千问）**：试点+字形识别 ≈ 80K；50 首全量 OCR 验证 = **236.6K**（image 210K + prompt 18.6K + completion 8K，qwen3-vl-plus）；合计 ≈ **320K tokens**。折算扩量成本：验证口径 ≈ 4.7K tokens/首（500 首 ≈ 2.4M）。
  - 遗留：① 5 行文本不匹配保守未迁（hymn 6/19/33/42）；② 其余 450 首待扩量；③ 实机 UI 截图因显示器休眠未拍（数据与单测已双验证）；④ 低音点目前仅 `2̣` 一个码位，其余级数待出现时补。

---

## 任务 4：全库扩量校准 + PPT 冗余表清除（2026-09-20）

- **提问**：用户指出 `hymn_jianpu` 表含 PPT 信息、怀疑库中有多余数据；要求备份→删冗余→按新方法（任务 3 的几何重建）调整全库曲谱→曲谱模式正常播放、谱词对应与 PDF 一模一样。
- **解决思路**：
  1. 探查确认两套曲谱数据并存：`hymn_jianpu(_line)`=PPT 管线（extractor=ppt_jianpu/1.0，notes 为 PPT 文本格式），`hymn_score*`=PDF 管线；App 代码对前者零引用 → 安全删除。
  2. 扩量前检查：473 首 PDF 全覆盖、全部有 char/lyric、未知码位仅 10 种 15 首、存在 `51_a` 多版本编号（曲目清单必须读表而非数字 range）。
  3. 渲染 10 种未知码位字形目视定案：真正进 primary 的仅 3 种组合字形（4ef6/4e73=高音4、4f62=高音3，时值短线不表征）；其余为爬虫已排除噪声。
  4. **发现 v1 apply 隐患**：整体清 primary 会把 nopair/mismatch 行清成非 primary → app 丢行。重写为增量式 apply2（只翻转确认迁移的行对 + 全库 primary 行 notes 重解码 + chars.note 按最终 code_seq 重解码）。
  5. 修复 v1 遗留 7 条孤儿挂词行（恢复 primary + 重解码）。
- **最终结果**：
  - 完成：备份 `tjc_hymn.bak-20260920_204722.db` → 全库 **629 跨行迁移 / 1339 原位 / 22 保守保留 / 3177 重解码** → DROP 两张 PPT 表 + VACUUM（9.8→7.9MB）→ 一致性检查全绿（char 孤儿=0、note_index 越界=0、`?`/`@` 残留=0、无 primary 曲=0、可显示谱行 2015）。
  - 改动：`data/tjc_hymn.db`、`hymn_app/lib/models/hymn_score.dart`（seed +3 码位）、`hymn_app/test/lyric_page_score_test.dart`（+1 用例组，42/42 过）、`docs/Windows/UI_CONFIRMATION.md`（5.13 节）；分析器全绿、release 构建通过、应用启动 Responding=True。
  - 验证：抽查 124_a 五条谱带与 DB 逐位一致（艺术字体 5 形近 6，目视须用无点数字行比对）；match 判据（y±16 字符流==code_seq）会吸入相邻声部行产生 62.6% 假阳性，不作验收依据。
  - 遗留：4 首结构不符曲与 22 保守保留行待人工个案处理；实机 UI 目视（显示器休眠）待用户确认；未提交 git（待用户确认）。



## 任务 5：曲谱记号全量补全·标点/小节线/附点/点位置（2026-09-20 晚）

### 提问

用户提交第 1 首第 1 节曲谱+歌词截图，对照 PDF 指出 5 项差距（每首都有）：① 歌词标点缺失；② 小节线缺失；③ 附点缺失；④ 时值下划线缺失；⑤ 高/低音点位置不正（偏右）。歌词简繁差异明确**不算**问题（保持简体）。

### 解决思路

- **① 标点**：官网爬虫 chars 只存汉字，但 PDF 文本层歌词带含全角标点 → 按 primary 行取歌词带，汉字序列校验（含数量一致的位置式回退）后重建 chars，标点作 `note_index=-1` 独立行；渲染端 `stanzaCells` 把标点并入前一字显示文本（不占列位，第 k 节文本同理）。全库 1941 行 / 6417 标点。
- **②③⑤ 记号**：`code_seq` 增量重建 v2——旧 seq 为骨架（已验证的音符主流程零回归），从 PDF `texttrace` 提取新记号插入。关键取证链：
  - rawdict 的 char bbox 是 em 框（固定 7×30.9），相邻声部行重叠，**不可作行归属判据**；
  - `texttrace.origin_y = 行 y + 8.7` 恒定（多行实测），**基线判据**干净分离四声部；
  - 小节线 origin 比音符基线低 ~4pt（窗 [0,6]）；附点同基线（|dy|≤3）；
  - **独立低音点与女低行的点几何同位**（点画在上一行基线+3，像素扫描证实）→ 女高行弃表征（真实低音已由组合字形 4e5f=2̣ 覆盖）；
  - 字母组合字形（t/y/e/j…）**每首歌字体子集 cmap 不一致**（4ee1 在第 2 首=5̣_、第 12 首=6_）→ 沿用旧 ALIAS 延音线口径，**时值下划线（任务④）留作专项**。
- **⑤ 渲染**：弃用组合字符 U+0307/0323（字体渲染偏右），`_SymCell` CustomPaint 自绘：高音点居中于数字上方、低音点下方、附点右侧中上、时值线 1~3 条在下、小节线在列右缘竖线。
- **附带修复**：行间列错位（每行 Row 独立居中 → 整页统一列网格左对齐，第 12 首 24 vs 19 元素行不再错位）。
- **元素格式**：`code_seq` 元素扩展 `'+'` 连接多码位（`4e59+5d3d+602d`=`5.|`），向后兼容；Dart 解码逐码位归一化再拼接（修复 `y|` 整体查别名失配导致显示 `y` 的 bug）。

### 最终结果

- 完成：备份 `bak-20260920_221834-pre-punct.db`（标点前）+ `bak-20260920_231006-pre-seq.db`（记号前）→ 标点 1941 行 → 记号重建 2600 行（514 headsmis / 70 词对位失败行保旧数据）→ 小节线 2435 行、附点 1811 行生效。
- 改动：`data/tjc_hymn.db`、`hymn_app/lib/models/hymn_score.dart`（seed +24 码位、`+` 解码、标点归并）、`hymn_app/lib/widgets/score_lyric_view.dart`（_SymCell 自绘、列网格）、`hymn_app/test/lyric_page_score_test.dart`（45/45）、`docs/Windows/UI_CONFIRMATION.md`（5.14 节）。
- 验证：`_check_full.py` 全绿；analyze 全绿；release 构建通过；**第 1 首、第 12 首实机截图逐小节对照 PDF 一致**（用户问题 ①②⑤ 均消除）。
- 遗留：① 任务④ 时值下划线需按首提取字形轮廓分类字母组合字形（独立专项，根因=每首子集 cmap 不同）；② 514 headsmis 行无新记号（保旧数据）；③ 61 简繁差异行未加标点；④ 未提交 git（含 DB 是否入库待用户决策）。

## 任务 6：PPT 字体编码方案取证（2026-09-21 续）

### 提问

用户提供 `简谱字体.ttf`（11KB）、`歌词字体.ttf`（5.1MB）、`001.ppt`（第 1 首官方 PPT）：PPT 中简谱以特殊字符编码（如 `1  1    3  3 \ 5/5/\6/6  6\5/3/\`、`!/5  5\6/3/\4  2  2.q\1///|`）经字体转换显示，与固定位置歌词和谐适配。问：能否参考此设计，用数据库保存歌词显示方式/简谱编码/字体转换，形成高效曲谱歌词输出？是否需要提供其他诗歌的 PPT？

### 解决思路（取证结论）

1. **简谱字体 cmap 全解**（`tools/_font_sheet.py` 渲染 94 码位字形表目视定案）：
   - 全宽 1.0em：数字 `1-7`、预组合字形（字母区 = 数字+高低音点+下划线数，如 `!`=高1、`%`=高5、`t`=5̲、`q`=1̲、`e`=3̣̲、`a`=1̣̲̲…）、延音线 `/`、小节线 `\`、终止线 `|`、双纵线 `?`、反复 `[` `]`、附点 `.`、休息符 `` ` ``/`p`；
   - **零宽 0.0em 叠加修饰符 24 个**：`'`高音点、`8`/`,`低音点、`+`/`=`/`,`下划线 1/2/3 条、`-`延音叠加、`(` `)` `0` `9` 连音线弧、`P`升/`:`降/`"` `L` 还原、`{` `}` 括号等；
   - 半宽 0.5em：空格（空列）。
   - **节奏自洽验证**：001.ppt 第 1 节解码 = |1 1 3 3|5— 5—|6— 6 6|5— 3—|… 每小节恰 4 拍（4/4），`5.t`=附点四分+八分、`5///`=全音符，语义完全确认。
2. **001.ppt 文本提取**（`tools/_ppt_dump.py`，扫 OLE 记录 TextBytesAtom 0x0FA0）：12 个文本原子 = 3 页 × （简谱行+歌词行交替+页码），编码串与用户示例逐字吻合。
3. **歌词字体 = DFKai-SB 標楷體**（Windows 自带，无需内嵌；Android 可内嵌 5MB 或换楷体）。
4. **数据源断档**：5.13 删除 `hymn_jianpu(_line)`（474/12136 行 PPT 编码）时的备份 `bak-20260920_204722.db` 为 **0 字节损坏**，其后备份均在 DROP 后 → **PPT 编码数据全库已丢失**；旧爬虫代码不在仓库、下载源未知 → **需要用户提供 PPT 源**。

### 最终结果（阶段结论，待用户决策后实施）

- **方案可行且优于现状**：字体原生承载下划线/高低音点/小节线/延音线/反复记号，任务④⑤类手工绘制问题整体消失；简谱字体仅 11KB 可内嵌 Flutter。
- 拟定架构：新表存 PPT 原始编码+歌词+坐标 → App 内嵌简谱字体直接渲染编码串（零宽字符自动叠加）→ 歌词行標楷體按 PPT 坐标比例对齐；无 PPT 的歌保留 PDF 解码渲染器兜底。
- **答复用户：是，需要其他诗歌的 PPT**（或告知原下载网站由爬虫批量获取）。
- 产物：`tools/_ppt_probe.py`、`_font_sheet.py`、`_ppt_dump.py`、`_lyricfont_probe.py`、`_jpfont_sheet.png/pdf`、`_ppt001.txt`（取证用，暂不入库）。
- 遗留：PPT 源获取方式待用户答复；阶段 A 概念验证（001.ppt → DB → Flutter 渲染对照 PDF）待批准启动。


#### 任务 6 实施结果（用户提供全库 PPT 后完成）

- **数据源**：用户给出 `E:\赞美诗\赞美诗--投影\`（475 个 .ppt = DB 474 首 + 库外第 500 首），命名 `001..469/500` + `051a/051b/124a/124b/132a/132b/274a/274b/296a/296b` → 映射 DB `1..469/500` + `51_a/51_b/124_a/124_b/...`（全match）。
- **提取器**（`tools/extract_ppt.py`，生产级，可重跑）：迷你 CFB 读取器（sector/FAT/DIFAT/miniFAT）→ 记录树遍历（容器递归 + **0x0FBA deflate 压缩容器**）→ 文本原子 UTF-16LE（失败回退 GBK）→ 页内行分类（页码/简谱行/歌词行/头行）→ 符号归一化（`︱`→`\`、`–/—/―`→`/`、`‐/−`→`-`、TAB→8 空格、`\x0b/\x0c` 断行）→ 空行对剔除。
- **结果**：475 首 / 3032 页 / 12674 行 100% 解析；警告仅结构性行（545「(二)/(副歌)」节标签行 + 77 仅谱行）；**未知码位 0**（全部落在简谱字体 cmap 94 码位内）；经历 4 轮修复（字节扫描→记录树、GBK 乱码→压缩容器、`︱`/`–` 符号、TAB/`\x0b` 隐藏字符）。
- **落库**：`hymn_ppt`(3032) + `hymn_ppt_line`(12674)；备份 `tjc_hymn.bak-20260921_080223-pre-ppt.db`。
- **字体量化**（PyMuPDF 实测 hmtx/cmap）：简谱字体 数字/符号 1em、空格 0.5em、**零宽修饰 24 个 0em**；歌词字体（標楷體 DFKai-SB）汉字 1em、半角 0.5em。
- **版式模型**：全库 6249 组「音符位置 ↔ 字位置」最小二乘拟合 → **字号比 R 中位数 = 2.000**（p10 2.00 / p90 2.10）；PPT 内 StyleTextPropAtom 解析字号 = 歌词 21.02pt、母版默认 10.28pt（≈1:2）互相印证。→ 渲染即「两行各一个 TextPainter + 字体自然 advance」，与 PPT 文本框同构。
- **App**：新增 `models/hymn_ppt.dart`（em 宽度规则 + 模型）、`widgets/ppt_score_view.dart`（CustomPaint 渲染）、`repo.hasPptTables/loadPptDoc`、`hymn_display` 曲谱模式优先 PPT 数据并回退 PDF 视图；内嵌 `assets/fonts/jianpu.ttf`(11KB, JianPu) + `lyric_kai.ttf`(5.1MB, EchoKai，因本机未装標楷體)；标题/歌词沿用简繁转换，编码串不转换。
- **验收**：`flutter analyze` 全绿；`flutter test` **59/59**（+14）；`flutter build windows --release` 通过；实机截图第 1 / 12 / 24 首：延音线/小节线/附点/**字体自带时值下划线**/连音线弧/终止线/高音点全部原生呈现（PDF 管线任务④⑤根因整体消失）。
- **遗留（待用户裁定）**：① 谱行与歌词行水平基准——PPT 内同框左对齐，但实测音符与「2×字位」有系统偏移（mean +2.4~3.9 谱 em、pstdev 1.8~3.0），疑段落级缩进或逐行手调；若要求与印刷 PDF 同级精确对齐，走「PPT 编码语义 + PDF 逐字对位」混合方案（需新增 PPT 元素↔PDF 元素序列对齐表）。② 旧 PDF 渲染器保留为无 PPT 数据时的回退。③ 475 个 PPT 与 DB 的 474 首对应（第 500 首无 DB 记录，已入库备用）。

