# 会话防中断通用策略写入全局规则会话

- 日期：2026-08-05（时间由回顾估算，本会话开始于约 21:55，以首个环境时间戳 21:55:42PM 推断；当日中断，2026-08-18 恢复后补写本日志）
- 工具链：Cline + DeepSeek API
- 涉及模块：全局会话规则（Cline 配置）

## 任务 1：按 Windows 11 + cmd 环境改写「会话防中断通用策略」并写入全局规则

### 提问

用户提供一大段「会话防中断通用策略」规则文本（含语言要求、13 条防中断策略、命令执行守则），要求：**"以上内容，根据系统环境，修改一下细节，写入你的全局规则中"**。

- 要求始终使用简体中文交流与注释；
- 策略覆盖：大命令拆小、限制工具输出量、超长任务后台运行+轮询、避免并发 git 操作、轻量校验优先、控制会话规模、严禁 `!` 字符、git 一律绝对路径 + `-C`、禁止 think 死循环、输出收敛、中断恢复、think 短平快、高耗时命令「脚本+输出文件+read」三件套，以及命令执行守则（不依赖 cwd、禁止长 `&&` 链、多步检查收敛为单脚本、临时脚本用完即删）。

### 解决思路

- **先读现状再动手**：读取全局规则 `C:\Users\小蔡爱金雪\.clinerules`，确认已有「PATH 修复」与「Python 偏好」两章节，必须保留；新内容以追加章节方式并入。
- **环境差异分析**：用户提供的原始规则面向 Linux/bash（`nohup`、`tail`、`pgrep`、`rm`、`/tmp`、`/home/zjx/hymn_crawler`），而本机为 **Windows 11，默认 Shell 为 cmd.exe**，因此逐条适配：
  - 输出限制：cmd 无 `tail` → 用 `powershell -Command "命令 | Select-Object -Last 20"` 接管；
  - 后台运行：`nohup ... &` → `start /b "" python ... > 日志 2>&1`；轮询 `pgrep -f` → `tasklist | findstr /I python.exe`；
  - 删除脚本：`rm -f` → `del /Q`；查看文件：`cat` → 禁止 `type`，改用 read_file 工具；
  - 路径示例：`/home/zjx/hymn_crawler` → `D:\项目路径`，临时日志目录用 `C:\Users\小蔡爱金雪\.cline\logs\`；
  - git 命令：保留绝对路径 + `-C` 写法，并补充"执行前先点引用 Fix-Path.ps1 修复 PATH"；
  - `!` 字符禁令：补充 cmd 在延迟变量展开（`cmd /V:ON`）下的同样风险。
- **方案选择**：新增章节较多且分散，用 `write_to_file` 整文件重写而非多处 `replace_in_file`，一次成型更稳妥；保留原有两章节原文不动。

### 最终结果

- **完成结论**：全局规则已按 Windows 11 + cmd 环境完成改写与写入，13 条防中断策略全部落地。
- **关键改动**：`C:\Users\小蔡爱金雪\.clinerules` 整文件重写——保留「PATH 修复」「Python 偏好」两章节，新增「语言要求」「会话防中断通用策略（13 条 + 命令执行守则）」章节。
- **验证方式**：write_to_file 返回成功；编辑器自动格式化补充了各章节空行；read_file 复核全文结构完整、无工具输出混入。
- **遇到的困难**：① 环境适配点琐碎（cmd 与 bash 命令差异逐一映射）；② 编辑器自动格式化会在章节标题与正文间插入空行，后续 SEARCH/REPLACE 需以格式化后的最终内容为基准。
- **遗留问题**：新规则对**新开的会话**生效；本会话因中断 12 天，恢复后按项目「会话日志强制流程」补写本日志。

---

## 会话总结

- 本次共处理 **1 个任务**（会话防中断通用策略 + 语言要求写入全局规则）
- 关键收获：
  1. **跨平台规则适配方法论**：同一条策略在 Linux/bash 与 Windows/cmd 下的命令实现差异需逐一映射（tail→PowerShell、nohup→start /b、rm→del、cat→read_file），并显式说明差异原因
  2. **写文件前必须先读现状**：全局规则已有 PATH 修复与 Python 偏好章节，避免盲目覆盖
  3. **中断恢复的轻量验证**：write_to_file 返回成功即视为落盘，先 read_file 复核再继续，不重复执行已完成步骤
- 遗留事项：
  - 新规则对后续所有新会话生效，本会话已结束
  - 项目「会话日志强制流程」制度已建立（见 `docs/sessions/README.md` 与 `_template.md`），后续会话须在任务开始时即创建日志文件，避免事后补写
