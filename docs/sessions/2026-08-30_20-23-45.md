# 五套换肤配色 + 换肤机制（v1.4.0）

- 日期：2026-08-30
- 工具链：Cline + DeepSeek API
- 涉及模块：UI 主题 / 标题栏 / 状态持久化 / 文档

## 任务 1：设计 5 套配色方案并提供效果图

### 提问

用户希望「丰富当前软件的界面效果」，期望提供 **5 套不同的配色方案**，要求配色好看且**方便换肤**，并询问能否提供效果图。

### 解决思路

1. **现状盘点**：颜色全部集中在 `hymn_app/lib/app.dart` 的 `AppColors` 静态常量类（17 个语义色槽），被 8 个文件约 170 处引用；另有少量硬编码（`Colors.white`、关闭按钮悬停红、标题栏按钮悬停灰、滚动条滑块灰）。
2. **换肤可行性的关键判断**：只要保持「同一套语义色槽、只换色值」，换肤就是零成本 —— 界面引用色槽名，切换调色板即整体换肤。据此设计 5 套方案，覆盖冷暖/明暗/色相多样化：
   - ① 晨光蓝（默认，与 v1.3.1 一致，零回归）
   - ② 暖阳金 · 圣堂（暖奶油 + 圣金黄 + 橄榄绿，贴合赞美诗气质）
   - ③ 静谧绿 · 草木（鼠尾草绿 + 薄荷白，护眼）
   - ④ 典雅紫 · 暮云（紫罗兰 + 青碧强调，晚祷静谧感）
   - ⑤ 暗夜墨 · 深色（深空灰蓝 + 明亮青蓝，夜读）
3. **效果图方案**：不使用静态 PNG，改为制作**交互式 HTML 高保真预览页** `docs/theme_preview.html`（1800×890 画布 = 基座 850 + 左栏 350 + 右栏 600），忠实还原标题栏/顶栏/三面板/播放条/状态栏，5 套配色一键切换 + 「对比全部」纵览 + 每套调色板明细（色名+hex），可缩放。比静态图更好对比、可长期维护。

### 最终结果

- **完成结论**：5 套配色方案已设计定稿，效果图以 `docs/theme_preview.html` 交付（浏览器打开即可预览）。
- **关键改动**：新增 `docs/theme_preview.html`（CSS 变量 = 语义色槽，与 Flutter `AppPalette` 字段一一对应）。
- **验证方式**：浏览器直接打开预览；色值均按语义对比检查可读性（文字/背景对比度）。
- **遇到的困难**：预览文件较大需分块写入；无。
- **遗留问题**：待用户选择/微调具体色值（改 HTML 与 `app_palette.dart` 两处即可）。

---

## 任务 2：Flutter 实现换肤机制

### 提问

用户要求配色「方便换肤」——即在应用内可随时切换并持久化。

### 解决思路

1. **架构方案对比**：
   - A. `ThemeExtension`（Flutter 惯用）：需把 170 处 `AppColors.xxx` 改成 `context` 取值，且 `const` 全部失效，改造量大、侵入性强。
   - B. **静态门面 + 当前调色板指针（选定）**：`AppColors` 保留同名静态 getter，内部读「当前调色板」；整棵 MaterialApp 用 `ValueListenableBuilder<AppPalette>` 监听重建，所有引用自动跟随。业务代码改动最小（仅需去掉被波及的 `const`）。
2. **持久化**：`state.json` 新增 `appTheme` 字段，`main()` 启动时恢复，HomeScreen 切换时保存。
3. **入口**：标题栏新增「调色盘」按钮（用户手册左侧），点击弹出 5 套配色菜单（主色圆点 + 名称 + 当前项对勾），点击即切换。

### 最终结果

- **完成结论**：换肤机制实现完成 —— 标题栏调色盘一键切换 5 套配色，重启保持。
- **关键改动**：
  - 新增 `lib/theme/app_palette.dart`：`AppPalette`（19 个语义色槽）+ 5 套预设 + `kThemes` + `themeById` + `ThemeController`（ValueNotifier）。
  - `lib/app.dart`：`AppColors` 由 static const 改为**当前调色板门面**（静态 getter）；`EchoHymnApp` 用 `ValueListenableBuilder` 监听重建 MaterialApp/ThemeData；滚动条滑块色随配色。
  - `lib/main.dart`：启动时读 `state.json` 恢复配色。
  - `lib/services/app_state_service.dart`：`AppState.appTheme` 字段 + `saveAll` 参数。
  - `lib/screens/home_screen.dart`：标题栏新增 `palette_outlined` 换肤按钮 + `_showThemeMenu`（showMenu 5 项）+ `_switchTheme`；`_saveState` 带配色；`_WindowButton` 悬停灰改 `AppColors.windowBtnHover`。
  - `lib/widgets/user_manual_dialog.dart`：手册新增换肤说明。
  - 76 处含 `AppColors` 的 `const` 表达式经 `tools/_fix_const.py` 脚本去 `const`（分析器逐项校正）。
- **验证方式**：`flutter analyze` 0 issues；`flutter build windows --release` 成功；启动冒烟测试正常（日志「主题: 晨光蓝」，数据库 474 首加载、无异常）。
- **遇到的困难**：`AppColors` 变动态后约 76 处 `invalid_constant`，手改量大 → 写一次性脚本按「const 表达式内含 AppColors」自动去 const，再用 analyze 收敛剩余 lint。
- **遗留问题**：待实机逐一验证 5 套配色的视觉细节与弹出菜单交互；可后续把「调色盘」入口补充到用户手册快捷键表或测试清单。

## 任务 3：分区极浅底色 + 配色全称（v1.4.0 完善）

### 提问

用户看了效果图很喜欢 5 套配色，但提出两点优化：

1. UI 已分区且有分割线，但希望**不同区域也有不同底色**——白主调风格下，用极浅灰、极浅蓝、极浅绿等同色相极浅色区分各区域（标题栏/顶栏/左栏/内容区/版本栏/播放条/右栏/状态栏）。
2. 配色名称使用**全称**（晨光蓝 · 经典 等）。

### 解决思路

1. **色槽扩展**：`AppPalette` 从 19 色扩展为 **25 色**，新增 6 个分区底色槽（titleBarBg/topBarBg/rightPanelBg/versionBarBg/playBarBg/statusBarBg），左栏沿用 sidebarBg、歌词区沿用 lyricsBg → 共 8 个分区底色。5 套方案各配同色相极浅层次（晨光蓝：#FFFFFF→#F5F8FF→#EFF4FC→#F2F5FB→#EAF1FC→#FBFCFE→#F4F6FB + 暖白歌词区；暗夜墨：相近深色层次）。
2. **应用点**：标题栏/顶栏/右栏/状态栏（home_screen.dart）、版本栏/播放条（hymn_display.dart）背景改读新色槽；按钮/卡片/弹窗保持 cardBg 白底不受影响。
3. **全称**：`kThemes.name` 改全称，菜单/Tooltip/日志/用户手册同步；HTML 预览页同步更新（CSS 变量 + 明细）。

### 最终结果

- **完成结论**：8 个分区极浅底色 + 配色全称均实现，预览页同步。
- **关键改动**：`lib/theme/app_palette.dart`（+6 色槽、5 套分区色、全称）、`lib/app.dart`（AppColors +6 getter）、`lib/screens/home_screen.dart`（标题栏/顶栏/右栏/状态栏背景）、`lib/widgets/hymn_display.dart`（版本栏/播放条背景）、`lib/widgets/user_manual_dialog.dart`（全称）、`docs/theme_preview.html`（分区底色变量+明细+全称）、测试清单新增 T23~T26。
- **验证方式**：analyze 0 issues；build 成功；启动冒烟正常；**截图像素采样**确认标题栏=白、顶栏=#F5F8FF、右栏=#F2F5FB、歌词区=#FDF8EE（其余为近白色，截图管线舍入不可分辨，属设计预期）。
- **遇到的困难**：CopyFromScreen 顶部被其他窗口遮挡导致采样异常 → 移窗到(60,60)+BringWindowToTop 解决；近白色（10 单位内）无法用像素采样区分，仅能确认非纯白区域。
- **遗留问题**：T23~T26 待用户实机肉眼复核各分区极浅色的观感是否满意（可微调 app_palette.dart 色值）。

## 任务 4：歌词区回归同色相层次（v1.4.0 完善②）

### 提问

用户指出遗留问题：晨光蓝经典模式的歌词区 `#FDF8EE`（暖白）当初是为「区分歌词区范围」引入的，现在与晨光蓝的极浅蓝层次风格不符；并要求**其他配色方案也重新梳理**，不要受这个暖白特例影响。

### 解决思路

1. **根因**：`#FDF8EE` 是「历史遗留特例色」——为了区分歌词区而用暖色调，破坏了「8 分区同色相极浅层次」的配色体系。
2. **原则**：歌词区应成为各方案**同色相层次**中的一档，而非特殊色。暖阳金因本身就是暖色系，暖黄纸感保留（不属特例）。
3. **调整**：晨光蓝歌词区 `#FDF8EE`→`#F2F6FD`（浅蓝白）；静谧绿 `#F0F9F4`→`#EEF8F2`、典雅紫 `#F5F0FB`→`#F1EBFA`（更贴主色相）；暖阳金 `#FDF4DE`→`#FCF4E0`；暗夜墨 `#20252F` 本就符合不动。同时微调相邻分区（左栏/右栏/版本栏/状态栏）确保相邻区域色差可感知，避免左栏与歌词区混淆。

### 最终结果

- **完成结论**：5 套方案的歌词区全部回归各自同色相层次，晨光蓝无暖色残留。
- **关键改动**：`lib/theme/app_palette.dart`（4 套歌词区 + 相邻分区色值）、`docs/theme_preview.html`（CSS 变量 + 明细）、`docs/UI_CONFIRMATION.md`/`SESSION_SUMMARY.md`（色值表）、`hymn_app/test/v140_theme_test_cases.md`（T23b 新增）。
- **验证方式**：analyze 0 issues；build 成功；**截图像素采样**歌词区 = `#F2F6FD` 与设计完全一致（蓝色调，非暖白）。
- **遇到的困难**：无。
- **遗留问题**：待用户实机确认晨光蓝歌词区蓝色调观感。

## 任务 5：v1.4.0 换肤首轮测试 6 项 NG 修复 + 复测清单

### 提问

用户完成了 `hymn_app/test/v140_theme_test_cases.md` 测试（26+ 项），共 **6 项 NG**，要求修复 bug 并创建复测清单。

### 解决思路（6 个 NG 根因与修复）

| NG | 根因 | 修复 |
| --- | --- | --- |
| T06 暗夜墨顶部白色带 | 窗口无标题栏（WS_CAPTION 移除）+ WS_THICKFRAME，Win11 DWM 在窗口顶部绘制约 7px 边框；`UpdateTheme` 的沉浸式深色模式只跟随**系统**主题——系统浅色+应用暗夜墨时 DWM 用浅色边框 `#F1F3F9` | native 新增 `SetAppearance`/`setWindowAppearance` 通道：随应用主题设置 `DWMWA_USE_IMMERSIVE_DARK_MODE` + `DWMWA_BORDER_COLOR`；启动与切色时同步 |
| T14 换肤后大部分区域不变色 | `MaterialApp(home: const HomeScreen())`——const 实例被 Element 复用，MaterialApp 重建不触发 HomeScreen build，仅依赖 Theme 的进度条变色 | `HomeScreen.build` 外包 `ValueListenableBuilder<AppPalette>` 监听 `ThemeController.notifier`，整树重建，所有 AppColors 引用即时更新 |
| T18 滚动条/标题栏悬停不随配色 | 同 T14（整树不重建） | 随 T14 自动解决 |
| T19/T26 暗夜墨弹窗输入框浅底浅色 | `ThemeData.light()` + `fromSeed` 生成的 colorScheme 为浅色 surface；暗夜墨下 Dialog/输入框仍浅底，而 textPrimary 是浅色 | `AppPalette` 新增 `brightness` 字段（暗夜墨=dark）；`EchoHymnApp` 按亮度选 `ThemeData.dark()/light()` + `fromSeed(brightness)` |
| T22 实机左右栏与预览不一致 | 左栏各面板列表行背景用 `AppColors.cardBg`（白），覆盖左栏浅蓝 `sidebarBg` | 三个面板的列表行/分类行/分页栏背景统一 `cardBg→sidebarBg`（脚本批量替换），左栏整体融入浅蓝，与预览一致 |
| T23 晨光蓝左栏列表内容异常 | 同 T22 | 同 T22 |

### 最终结果

- **完成结论**：6 项 NG 全部修复；`flutter analyze` 0 issues；构建成功。
- **关键改动**：
  - `lib/screens/home_screen.dart`：build 包 ValueListenableBuilder（换肤整树重建）+ `_syncWindowAppearance`（启动/切色同步窗口外观）
  - `lib/theme/app_palette.dart`：新增 `brightness`（暗夜墨=dark）
  - `lib/app.dart`：按亮度选 ThemeData.dark/light + fromSeed(brightness)
  - `windows/runner/{win32_window.h,cpp, flutter_window.cpp}`：`SetAppearance`（immersive dark + border color）通道
  - `lib/widgets/panels/*.dart`：左栏列表行/分页栏 cardBg→sidebarBg
  - 测试清单 NG 行标注修复；新增 `hymn_app/test/v140_theme_retest_checklist.md`（R01~R14）
- **验证方式**：analyze 0 issues；build 成功；截图像素采样确认——① 暗夜墨顶部边框 #F1F3F9→深色（DWM 深色模式生效）② 晨光蓝左栏 = #EBF1FA、歌词区 = #F2F6FD 与设计一致。
- **遇到的困难**：T06 白带多次验证反复——PrintWindow 固定渲染边框色不可信、DWM 属性需逐层排查；最终确认是「沉浸式深色模式未随应用主题」这一根因。CopyFromScreen 被窗口遮挡干扰采样，需 SetWindowPos 置顶解决。
- **遗留问题**：R01~R14 待用户实机复测（尤其 T14 切色全变、T19 弹窗输入框这两项需交互验证）。

## 任务 6：二轮复测 R03 修复（未选中控件底色 controlBg）+ 二轮复测清单

### 提问

用户完成复测清单测试：R01~R14 中 **仅 R03 NG**——「左右栏切换按钮、人声版、未选择的歌词切换按钮，感觉颜色没有严格匹配主题颜色」。要求判断是否值得修复。

### 解决思路

1. **判断：值得修复**。用户反馈的「次级控件未选中态」（顶栏切换按钮、版本按钮、模式按钮）旧实现用 `cardBg`（纯白）/ `sidebarBg`（侧栏色）做背景——暖阳金下冷白突兀、暗夜墨下白色刺眼、暗夜墨 sidebarBg 过深，均与主题色相不协调。
2. **方案**：`AppPalette` 新增 **`controlBg`（未选中/未激活控件底色）** 语义槽（25→26 色），5 套各配同色相浅色（晨光蓝 `#EDF3FB` / 暖阳金 `#F7EFDC` / 静谧绿 `#EAF4EE` / 典雅紫 `#ECE6F8` / 暗夜墨 `#262C38`），值介于所在栏背景与版本栏背景之间——按钮可辨且带主题色相。
3. **应用**：`_toggleButton` / `_versionBtn` / `_modeBtn` 三处未选中背景统一 `controlBg`；选中态 primary/selectedBg 不变；HTML 预览同步（CSS `--control-bg` + mockup toggle/pill + 明细）。

### 最终结果

- **完成结论**：R03 修复完成，未选中控件严格匹配主题。
- **关键改动**：`lib/theme/app_palette.dart`（+`controlBg`，26 色）、`lib/app.dart`（getter）、`lib/screens/home_screen.dart`（顶栏切换按钮）、`lib/widgets/hymn_display.dart`（版本/模式按钮）、`docs/theme_preview.html`（变量/mockup/明细）、`hymn_app/test/v140_theme_retest2_checklist.md`（新增 R15~R23）。
- **验证方式**：analyze 0 issues；build 成功；千问视觉确认顶栏切换/钢琴版/未选中模式按钮均为**淡蓝底**（带主题色相、比栏背景略深），非纯白。
- **遇到的困难**：像素采样坐标难对齐（窗口展开后顶栏覆盖全宽、按钮在两端），改用千问视觉整体确认。
- **遗留问题**：R15~R23 待用户二轮复测。

## 任务 7：三轮前复测 R15/R16 修复（未选中控件描边 controlBorder）+ 三轮复测清单

### 提问

用户完成二轮复测清单测试：**R15a/b/c、R16b 共 4 项 NG**，反馈一致——「controlBg 比背景深一档，但还不是肉眼可见的区分，建议按钮的边框色可以再深一些，形成直观的对比」。

### 解决思路

1. **判断：值得修复**。用户要求「直观对比」——1px 语义边框即可达成，且与预览 mockup（按钮本就有边框）一致。
2. **方案**：`AppPalette` 新增 **`controlBorder`（未选中控件边框色）**（26→27 色），5 套配明显深色描边（晨光蓝 `#98A1B2`/暖阳金 `#A2947A`/静谧绿 `#95A89D`/典雅紫 `#A095BA`/暗夜墨 `#3E4654`）；`_toggleButton`/`_versionBtn`/`_modeBtn` 的 Material `shape` 加 1px `BorderSide`（选中态 transparent 无边框）。
3. **验证陷阱**：flutter 增量 build 未更新 exe（旧 exe 导致千问/像素扫描均显示无边框）→ `flutter clean` 全量重建后，PrintWindow 像素扫描确认顶栏左侧按钮（x[14..78]×y[54..90]）与版本栏/模式按钮形成**完整矩形描边轮廓**。

### 最终结果

- **完成结论**：R15a/b/c、R16b 修复完成，未选中按钮有直观描边。
- **关键改动**：`lib/theme/app_palette.dart`（+`controlBorder`，27 色）、`lib/app.dart`（getter）、`lib/screens/home_screen.dart` + `lib/widgets/hymn_display.dart`（三处按钮 shape 加描边）、`docs/theme_preview.html`（变量/mockup/明细）、`hymn_app/test/v140_theme_retest3_checklist.md`（新增 R24~R32）。
- **验证方式**：analyze 0 issues；clean 全量 build；PrintWindow 像素扫描确认 `#98A1B2` 描边像素成矩形框（顶栏按钮/版本栏/模式按钮）。
- **遇到的困难**：flutter 增量 build 未更新 exe 导致误判「边框未渲染」，clean 全量重建后确认正常。
- **遗留问题**：R24~R32 待用户三轮复测。

## 任务 8：三轮复测全部通过 + 换肤功能验收（tag v1.4.0）

### 提问

用户完成三轮复测清单测试（全部 OK），要求完成换肤功能、打 tag、提交并推送。

### 最终结果

- **三轮复测**：R24a~R25c（描边修复项）+ R26~R32（回归）共 **12 项全部 OK，无 NG**——换肤功能验收完成。
- **验收要点**：5 套配色可切换/持久化/即时生效；未选中控件描边直观；暗夜墨深色模式正常、顶部无白带；预览与实机一致。
- **收尾**：复测清单补「验收结论」；会话日志归档；SESSION_SUMMARY 里程碑标注 v1.4.0 tag；提交后打 `git tag v1.4.0` 并推送 main + tag。
- **遗留问题**：无（换肤功能完整交付；Android/鸿蒙仍为占位）。

## 会话总结

- 本次共处理 8 个任务
- 关键收获：①「语义色槽」设计让换肤零成本（27 色槽、5 套方案）；②const 实例在 MaterialApp 重建时不重跑 build（HomeScreen 自监听整树重建）；③深色主题必须用 ThemeData.dark()；④Win11 无标题栏窗口白带需随应用主题设 immersive dark mode；⑤未选中控件需 controlBg（底色同色相）+ controlBorder（深色描边）；⑥**验证前先确认 exe 时间戳/增量构建是否生效**；⑦「语义色槽 + 交互式 HTML 预览」让换肤方案的确认-实现-验收闭环高效完成。
- 遗留事项：换肤功能已验收并打 tag v1.4.0；后续可继续 Android/鸿蒙适配等。
