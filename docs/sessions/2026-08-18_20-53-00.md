# Cline 会话启动上下文最小化方案 + 另一设备启动提示词

- 日期：2026-08-18
- 工具链：Cline + DeepSeek API
- 涉及模块：Cline 环境配置（MCP 常驻 / 规则文件职责 / 启动流程）

## 任务 1：阅读 docs/sessions 提炼上下文最小化方案

### 提问

1. 阅读 `docs/sessions` 目录下的文档，关于 Cline 会话启动时如何减少上下文，提炼出方案；
2. 另一台设备同样是 VS Code + Cline 开发模式，需要精准、有效的提示词，让 Cline 在启动阶段实现最少的上下文。

### 解决思路

- 通读 sessions 目录：README（规范）、_template（模板）、5 个关键会话日志（08-16 的 3 个优化会话 + 08-17 机制建立 + 08-04 MCP 排障），重点提取上下文消耗的实测数据与优化措施演进史。
- 实测数据链：
  - 08-16 14-14 会话：系统提示 ~114K，大头为 4 个大型 MCP（powerpoint 100+ 工具 / playwright 40+ / xmind 40+ / web-bridge）；文件读取仅 ~10K → 方案 1+3（禁用大 MCP + 启动动作最小化）。
  - 08-16 14-24 会话：拆解增量——读 SESSION_SUMMARY +114.7k（其中约 114k 是会话一开始 MCP 工具定义常驻注入，文件本身仅 ~0.7k）、UI_CONFIRMATION +2.8k、git log +3.3k → 方案 B 精简常驻 7→2（仅 fetch + memory），基础上下文 130k → 33k。
  - 08-16 14-46 会话：验证启动全流程仅约 22K tokens（2.2%），目标达成。
  - 规则职责修正：全局 .clinerules 只放通用原则；项目专属启动路径放项目级 .clinerules。
- 提炼方案要点：①MCP 精简（最大头）②启动动作最小化（禁止重复读已注入的规则）③规则文件职责分离 ④大文件按需/关键章节读取 ⑤会话日志机制固化（本次按流程执行）。

### 最终结果

- **完成结论**：方案提炼完成，产出 `docs/CLINE_CONTEXT_MINIMIZE.md`（通用方法论 + EchoHymn 落地现状），并为另一设备撰写精准启动提示词（含 .clinerules 模板 + 首条提示词 + 自检清单）。
- **关键改动**：新建 `docs/sessions/2026-08-18_20-53-00.md`（本日志）、`docs/CLINE_CONTEXT_MINIMIZE.md`（方案文档）。
- **验证方式**：方案数据均来自 sessions 日志实测记录；提示词字数控制在最小但信息完整。
- **遇到的困难**：无。
- **遗留问题**：另一设备若为不同项目，需把 EchoHymn 专属路径替换为对应项目路径。

## 会话总结

- 本次共处理 1 个任务
- 关键收获：
  1. 上下文消耗 94% 来自 MCP 工具定义常驻注入，文件读取只占零头——优化优先改 MCP 配置而非文档。
  2. 实测数据表明精简后启动可压至 ~22K（约为原来的 1/6）。
  3. 启动提示词要「命令式 + 路径明确 + 禁止清单」，既保证信息完整又杜绝 Agent 自行扩大读取范围。
- 遗留事项：无。
