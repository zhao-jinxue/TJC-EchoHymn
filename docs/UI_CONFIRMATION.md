# ✅ UI 还原确认单

> 本文件是我根据您的 `docs/UI_DESIGN_TEMPLATE.md` 描述 + 数据库实际结构，整理出的「我将如何还原」的逐条确认清单。
> **请您逐节核对**，在需要修改/澄清的地方批注，确认无误后我即开始编码（目标还原度 95%+）。

---

## 0. 数据库实况（已探查 `data/tjc_hymn.db`）

| 表 | 内容 | 关键字段 |
| --- | --- | --- |
| `tjc_hymn` | 474 首诗歌 | hymn_number, title(繁体), lyricist, composer, source_info(源考), verse_1~10, staff_img_path/ staff_png_path(五线谱), numbered_img_path/ numbered_png_path(简谱), audio_versions(JSON: 鋼琴版/人聲版 → m4a/mp3), audio_version_list(JSON 数组) |
| `hymn_category` | 45 条分类 | **category(一级) → subcategory(二级)**，hymns(JSON: [{标题:编号}...])，分类文字为**简体** |

> ⚠️ **发现的关键差异**：您描述中提到「默认歌单根据**三级**分类目录」，但数据库中分类**实际只有两级**（一级 category ~ 14 类：颂赞/信心/圣工/圣徒本分/圣礼/婚丧礼仪/灵交/灵修/盼望/耶稣恩功/耶稣恩宠/附录/其他；二级 subcategory ~ 45 个，如 赞美天父/赞美耶稣）。请确认下方问题 ①。

---

## 1. 应用概览（已确认）

| 项 | 我读取到的 |
| --- | --- |
| 应用名称 | EchoHymn（顶栏显示「EchoHymn · 聆听赞美诗」） |
| 目标平台 | Windows 桌面优先（1280×800，最小 1000×700）+ Android + Web 响应式 |
| 语言 | 简体中文为主，**支持繁→简转换**（显示诗歌标题/歌词/源考时转换） |
| 字体 | 中文：系统默认（Windows 微软雅黑 / 思源黑体替代），标题 Bold |

---

## 2. 整体布局（已确认）

```text
┌────────────────────────────────────────────────────────────┐
│ [◀]  EchoHymn · 聆听赞美诗                [▶]   (顶栏 40px)
├──────────┬─────────────────────────────────┬───────────────┤
│ 左侧栏    │  主内容区                        │ 右侧栏         │
│ 宽 400px │  [钢琴版][人声版▾] [歌词][简谱][五线谱] │ 宽 400px      │
│          │  歌词 / 谱面显示区                 │ 诗歌源考       │
│ 按钮1 诗歌列表 │  播放控制: [⏮][⏯][⏭] 进度条     │ (可在顶栏收起) │
│ 按钮2 默认歌单 │                               │              │
│ 按钮3 个人歌单 │                               │              │
│ 按钮4 ＋新建   │                               │              │
├──────────┴─────────────────────────────────┴───────────────┤
│                     底部状态栏 (40px)                       │
└────────────────────────────────────────────────────────────┘
```

| 区域 | 我读取到的细节 |
| --- | --- |
| 顶栏 | 高 40、白底；左侧「◀」收起/展开左侧栏；中央 Logo+名称；右侧「▶」收起/展开右侧栏 |
| 左侧栏 | 宽 400、底 #F5F6FA；顶部一排按钮（高30×宽60） |
| 左侧按钮1「诗歌列表」 | 按**编号顺序**显示全部 474 首，**不显示**下方「＋新建歌单」按钮 |
| 左侧按钮2「默认歌单」 | 按分类目录显示诗歌歌单，**不显示**「＋新建」 |
| 左侧按钮3「个人歌单」 | 显示个人创建的歌单，**显示**「＋新建歌单」按钮 |
| 左侧按钮4「＋新建歌单」 | 弹出「个人歌单创建弹窗」 |
| 主内容区 | 底 #FAFBFC；顶部一排切换按钮 + 歌曲播放按钮（30×30）+ 进度条 |
| 主按钮1「钢琴版」 | 切换钢琴版音频 |
| 主按钮2「人声版」 | 切换人声版音频；**若有多个人声 → 变为下拉菜单选择** |
| 主按钮3「单歌词版」 | 歌词显示模式：纯文字歌词 |
| 主按钮4「简谱版」 | 歌词显示模式：简谱（数据库 `numbered_*` 字段或 lyrics 谱面） |
| 主按钮5「五线谱版」 | 歌词显示模式：五线谱（数据库 `staff_*` 字段） |
| 播放控制 | 上一首 / 播放暂停 / 下一首（30×30）+ 进度条 |
| 右侧栏 | 宽 400、底 #F5F6FA；显示**诗歌源考**（source_info 繁体内容，可繁转简） |
| 底部状态栏 | 高 40、白底、状态信息 |

---

## 3. 弹窗（已确认）

### 3.1 个人歌单创建弹窗（480×320）

- 顶部标题「个人歌单创建」
- 歌单名称输入框（**最多 30 字**）
- 搜索框（输入编号/名称，点击搜索按钮或回车 → 弹出「诗歌弹窗」）
- 中部：已添加诗歌列表（每项 = 编号 + 标题 + 减号，点击减号移除）
- 底部：「取消」「创建」按钮（创建 → 写入数据库 → 回到个人歌单列表）

### 3.2 诗歌弹窗

- 顶部标题「诗歌展示」
- 编号 + 诗歌名称
- 歌词区域（展示简谱歌词）
- 底部：「取消」「添加」按钮（添加 → 该诗加入当前创建的歌单并出现在已添加列表）

---

## 4. 全局规范 & 主题（已确认）

- 主色 `#2B6AE0`，悬停 `#1E57C8`，强调 `#00A870`
- 页面背景 `#FAFBFC` / 卡片 `#FFFFFF` / 侧栏 `#F5F6FA` / 顶底栏 `#FFFFFF`
- 一级字 `#1F2329`、二级 `#646A73`、三级/占位 `#8F959E`、分割线 `#E5E6EB`、边框 `#D0D3D9`
- 按钮：主蓝底白字 / 次白底灰边；圆角 8（按钮/卡片）、12（弹窗）、4（标签）
- 输入框：高 36、常态边框 `#D9D9D9`，聚焦主题色+外发光，圆角 6
- 列表项：高 30、悬停 `#F5F6FA`、选中 `#E8F0FE`+左侧 3px 主题色竖条
- 弹窗：遮罩 rgba(0,0,0,0.4)，右上角 ✕ 关闭，按钮在下方
- Toast：深色圆角条，2 秒，淡入淡出
- 滚动条：8px、滑块 `#C1C1C1`、轨道透明、滚动时显示
- 图标：Material Icons，24px
- 间距：8pt 网格（4/8/12/16/24），页面边距 24
- 动效：页面直接切换；按钮按下缩小 0.98+变深；加载居中转圈；空态「大图标+暂无数据」；出错「重试」；**无暗色模式**
- 响应式：最小 1000×700；窗口变窄侧栏收成图标/自动隐藏；变宽内容居中、区域按比例扩展

---

## 5. 数据方案（已确认 + 待定）

| 项 | 我读取到 / 建议 |
| --- | --- |
| 数据源 | 本地 SQLite（`tjc_hymn`、`hymn_category`）+ 本地文件（音频/谱图） |
| 列表加载 | 歌单一**次性**加载；诗歌列表**分页**加载 |
| 搜索 | 按编号 / 标题 |
| 个人歌单 | **数据库目前无此表**，需要新增（建议 `playlist` + `playlist_hymn` 关联表） |

---

## ✅ 用户确认结果（2026-08-15）

| # | 问题 | 用户决定 |
| --- | --- | --- |
| ① | 分类层级 | **选 A**：用现有两级「category → subcategory」（第三级即 hymns 内的诗歌内容） |
| ② | 繁→简转换 | **选 A**：显示层动态转换，引入 **OpenCC**，数据库内容不动 |
| ③ | 个人歌单 | 表结构：`playlist(id, name, created_at)` + `playlist_hymn(playlist_id, hymn_list[{hymn_id: name}], sort_order)`；**不允许重复添加** |
| ④ | 播放行为 | 当前列表（诗歌列表或当前歌单）内**循环切换**；底部状态栏显示：**诗歌编号 + 歌名 + 作词 + 作曲 + 播放进度** |
| ⑤ | 技术形态 | **重做现有 `hymn_app`（Flutter）界面**；可迁移/复制的本地运行程序；SQLite 数据库，各系统兼容（Windows 读 `data/tjc_hymn.db`，Android/iOS 内置） |
| ⑥ | 源考显示 | **繁转简后显示**，内容较长时**滚动** |

---

## ❓ 已确认的问题（作答归档）

---

## 📌 确认方式

请在以上逐条核对，直接回复：

1. 「确认单无问题，开始编码」
2. 或指出需要修改的**编号**（如问题①选 A、问题②选 C），我会更新确认单后开始。

> 一旦确认，我将按此文档 100% 还原 UI，并有 95% 以上的细节保真度。

---

## 第 3 轮修订（最终设计定稿 2026-08-16）

> 以下为当前实际实现的**最终标准**，以此为准：

| 项 | 最终设计 |
| --- | --- |
| 顶栏收起/展开按钮 | 未展开=**蓝底+展开箭头**（左栏左箭头/右栏右箭头）；已展开=**灰底+收缩箭头**（左栏右箭头/右栏左箭头），附悬停 Tooltip |
| 窗口最小尺寸 | **1202 × 700**（= 左栏 350 + 歌词 450 + 右栏 400 + 分隔线），歌词区最小 450×450 不被侧栏侵占；屏幕不足时最大化并整体等比内缩 |
| 左侧栏 | 默认与最小宽度 **350px** |
| 音频播放 | Windows 依赖 audioplayers（Media Foundation）`DeviceFileSource` 直读文件路径（m4a/mp3 解码；已替换 just_audio_windows）；失败 Toast 显示具体错误 |
| 人声版本 | 内容区=「钢琴版｜**人声版**」按钮组（人声版切至第 1 个人声版）；**多版本列表**在播放条「下一首」右侧（间距 24px，仅 >1 时显示，悬停提示 10 秒） |
| 状态持久化 | 歌单类型/二级目录/个人歌单/诗歌编号/音频版本/歌词模式——**切换即保存**（非仅关闭时），下次启动恢复；首次默认第 1 首 |
| 诗歌列表分页 | 默认每页 **35 首**，底部上一页/下一页 + 页码 |
| 全局滚动条 | 厚度 **10px**，滑块 #C1C1C1，轨道透明 |
| 歌词字号 | 按显示区宽高与歌词行数估算，以**铺满歌词区**为目标（非缩放被动调整） |
| 繁→简 | 歌词、诗歌列表、分类、源考均经 OpenCC 转为简体显示 |
| 默认歌单 | 一级分类 → 二级目录 → 点击二级目录展示该目录诗歌列表（带返回） |

---

## 第 4 轮修订（当前最新实现 v1.2.x，2026-08-21）

> 以下为**实际当前代码**的最新标准，与第 3 轮修订的差异以本节为准：

| 项 | 当前实现 |
| --- | --- |
| 基座画面 | **850×890 物理像素**（`kBaseWindowWidth/kBaseWindowHeight`）；歌词区永远保持 850 物理宽，不被侧栏挤压 |
| 侧栏模式 | **抽屉式向两侧扩展**：展开时窗口整体加宽，基座画面中心始终对齐屏幕工作区中心；左栏 **350px** / 右栏 **600px** |
| 侧栏状态 | 默认**收起**（最简播放画面 850 宽）；展开/收起状态持久化到 `state.json`（`showLeft/showRight`），启动自动恢复 |
| 窗口缩放 | 整棵 UI 树 `Transform.scale` **等比缩放**（contain，不裁切不变形）；最大化/手动 resize 时内容放大铺满；native resize 完成后重建一次避免时序竞态 |
| 窗口最小尺寸 | 客户区最小 **850×890** 物理像素（`WM_GETMINMAXINFO` 限制）；屏幕不足时最大化并等比内缩 |
| 顶栏按钮 | 未展开=**蓝底+展开箭头**（左栏 ◀ / 右栏 ▶）；已展开=**灰底+收缩箭头**（左栏 ▶ / 右栏 ◀）；附悬停 Tooltip |
| 左侧栏 | 三个 Tab（诗歌列表 / 默认歌单 / 个人歌单），各自由 `LeftPanel` 子类面板实现 |
| 诗歌列表 | 每页 **35 首**，底部上一页/下一页 + 页码；搜索框 = 定位跳转（编号即时定位 / 标题匹配列表 / ×保持定位） |
| 音频播放 | audioplayers `DeviceFileSource` 直读本地中文路径（Windows Media Foundation）；失败 Toast 显示具体错误 |
| 人声版本 | 版本栏「钢琴版｜人声版」按钮组；多版本列表在播放条「下一首」右侧（仅 >1 显示，悬停提示 10 秒） |
| 简繁转换 | **纯 Dart 查表**（数据库全量字符映射，繁→简 1052 / 简→繁 1025），弃用 OpenCC FFI |
| 状态持久化 | `state.json`（exe 同级，原子写 + 串行队列）；`leftTab/subcategory/playlistName/hymnNumber/audioVersion/displayMode/playlistIndex/showLeft/showRight`；重启恢复不自动播放 |
| 日志系统 | `LogService` → exe 同级 `logs/`（UTF-8 BOM，保留 7 份；FlutterError/平台异常全局捕获） |
| 歌词显示 | 歌词 / 简谱 / 五线谱三种模式；简谱/五线谱 `InteractiveViewer` 可缩放拖动；歌词字号按显示区自动铺满 |
| 个人歌单弹窗 | 新建/修改复用同一弹窗（520×420），名称限 30 字、按编号去重、修改模式含删除歌单；编辑保存后同步播放顺序 |
| 滚动条 | 厚度 **10px**，滑块 #C1C1C1，轨道透明 |

---

## 第 5 轮修订（当前最新实现 v1.3.0，2026-08-30）

> 以下为**实际当前代码**的最新标准，与第 4 轮修订的差异以本节为准。

| 项 | 当前实现 |
| --- | --- |
| 窗口标题栏 | **自绘窗口标题栏 30px**（移除系统 `WS_CAPTION`）：左侧 **logo（蓝色音符）+ 应用名「EchoHymn · 聆听赞美诗」**；右侧按钮组 = **用户手册（？） / 最小化 / 最大化·还原 / 关闭**（用户手册位于最小化按钮**左侧**）；保留系统缩放边框（WS_THICKFRAME）与最小/最大化样式（可 Win+方向键、拖到屏幕顶部吸附最大化） |
| 布局（四栏） | 自上而下 = **标题栏 30px → 顶栏 40px → 内容区 → 底部状态栏 30px**（状态栏原 40 改 30）；窗口最小客户区 **850×890 不变**，四部分在其内分配；标题栏与顶栏间有 1px 分隔线 |
| 顶栏（功能栏） | 左右两端 = 侧栏展开/收起按钮；中央显示**当前播放/选中歌曲**（"第 N 首《标题》"，繁体转简体），无歌时显示应用名；切歌自动刷新 |
| 标题栏拖拽 | 顶栏空白处按住拖动窗口（native `ReleaseCapture + WM_NCLBUTTONDOWN(HTCAPTION)`）；双击标题区最大化 / 还原；最大化状态由 native `WM_SIZE` 推送（`onWindowMaximizedChanged`）切换按钮图标 |
| 窗口按钮样式 | 42×40、无边框、悬停变灰（#E5E6EB）；关闭按钮悬停变红底白字（#E81123）；带 Tooltip |
| 全局快捷键 | **通用方案**：空格 / Ctrl+P 播放·暂停；Ctrl+→ 或 Alt+→ 下一首；Ctrl+← 或 Alt+← 上一首；Ctrl+↑ / Ctrl+↓ 音量增减；Ctrl+M 静音切换；F1 打开用户手册；键盘媒体键（播放暂停/上一首/下一首/音量/静音）直接控制 |
| 快捷键细节 | 播放/暂停、切歌、静音等**切换类动作忽略长按重复**；音量允许长按连续调节；**输入框获得焦点时空格放行输入**（不误触发播放） |
| 用户手册弹窗 | 顶栏「？」按钮或 F1 打开：**软件介绍 / 操作说明 / 快捷键说明**三部分；Esc 或右上角 ✕ 关闭；快捷键表与实现共用 `kShortcutList` 常量 |
| 音量控制 | `AudioService` 新增音量/静音（快捷键用，不持久化到 state.json） |
