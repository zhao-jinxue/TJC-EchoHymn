# V1.7.4 软著材料清单与待补项

> 目录：`docs/ruanzhu/v1.7.4/`（本目录）。生成器在 `tools/`，版本随 `pubspec.yaml` 单源。

## 一、已生成材料

| 文件 | 说明 | 状态 |
| --- | --- | --- |
| `源程序鉴别材料_V1.7.4.docx` | 前后各 30 页 × 50 行（全程序 10857 行 / 218 页），页眉 = 名称+版本+页码域；等宽字体 | ✅ 可直接提交 |
| `源程序鉴别材料_V1.7.4.txt` | 同上纯文本版（含每页行号区间，便于核对） | ✅ 留档核对用 |
| `软件说明书_V1.7.4.docx` | 10 章操作手册体例；页眉同口径；插图 2 幅（曲谱网格版式） | ✅ 可直接提交 |
| `受理中申请的处理建议.md` | 已受理未下证情形的三方案取舍（推荐：原版继续 + V1.7.4 另报） | ✅ |
| `版本变更说明_1.6.x至1.7.4.md` | 版本时间线 + 界面/数据差异 + 对鉴别材料的影响 | ✅ |
| `署名与版本一致性核对.md` | 名称/版本号在各载体的一致性核对 + 生成器重跑口径 | ✅ |
| `img/fig2_score_single_voice.png`、`img/fig3_score_block_width.png` | 说明书插图（组件级版式图） | ✅ |

## 二、提交包建议结构（自办或交代理）

1. 软件著作权登记申请表（版本号 **V1.7.4**）；
2. 源程序鉴别材料（本目录 docx）；
3. 软件说明书（本目录 docx）；
4. 著作权人身份证明（个人：身份证复印件签名）；
5. （委托代理时）代理委托书；
6. 随附：《版本变更说明》《署名一致性核对》供代理/补正参考（非必需件）。

## 三、待补项（可选，提升说明书完整性）

实机整屏截图 4~6 张（当前说明书仅有组件级版式图）：

1. 歌词模式整屏（含左栏/播放条）；
2. 简谱模式整屏（扫描图缩放后）；
3. 五线谱模式整屏；
4. 换肤菜单展开；
5. 字号菜单展开；
6. 搜索面板命中结果。

补图方式：把截图放入 `docs/ruanzhu/v1.7.4/img/`（命名 `fig4_…png` 起），
在 `tools/make_ruanzhu_manual.py` 对应章节加一行 `figure(...)` 后重跑即可。

## 四、注意事项

- 源程序鉴别材料**只含自写代码**（`hymn_app/lib/**/*.dart`）；第三方 pub 包/引擎代码不纳入；
- 说明书中诗歌文本/曲谱图/音频的权属表述已与 `LICENSE` 对齐（归原权利人，本软件为工具）；
- 版本升级后重跑两个生成器即可得到新版本材料（页眉版本自动跟随）。
