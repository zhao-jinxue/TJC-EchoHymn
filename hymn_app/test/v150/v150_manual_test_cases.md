# EchoHymn 用户手册优化测试清单（M01~M15）

> **功能**：手册内容重构（无版本概念 + 完整操作说明 + 自述打开方式）+ 启动自动弹出 + 「启动时显示本手册」勾选框
> **测试平台**：Windows（`echo_hymn.exe`）
> **测试方法**：人工/手工测试
> **测试准备**：① 从 `release/` 最新版运行；② 每项如实填写「实际结果（OK/NG）」，NG 记录具体现象。

## 一、内容规范

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| M01 | 无版本词汇 | 通读手册全文 | 全文无「新增 / v1.x / 新版本 / 新方法」等版本化词汇与句式 | | |
| M02 | 操作说明完整 | 对照软件实际功能逐项核对 | 覆盖全部界面区域：手册开关 / 标题栏 / 字号 / 配色 / 顶栏 / 左侧栏三栏目 / 搜索 / 音频版本 / 显示模式 / 播放条音量 / 右侧栏源考 / 状态栏 | | |
| M03 | 自述打开方式 | 查看操作说明首条 | 明确说明「？」按钮 / F1 / 启动自动弹出三种打开方式与 Esc / ✕ / 遮罩关闭方式 | | |

## 二、启动弹出与勾选框

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| M04 | 启动自动弹出 | 升级后首次启动 / 每次启动 | 主界面出现后自动弹出用户手册 | | |
| M05 | 取消不再弹出 | 勾选框取消 → 关闭软件 → 重启 | 重启后**不再弹出**手册 | | |
| M06 | 勾回恢复弹出 | 「？」或 F1 打开手册 → 重新勾选 → 重启 | 重启后恢复自动弹出 | | |
| M07 | 持久化生效 | 勾选切换后查看 exe 同级 `state.json` | `manualOnStart` 键即时更新（false/true），其他字段不丢失 | | |
| M08 | 整行可点 | 点勾选框文字/行空白处 | 与点框体同样切换状态 | | |
| M09 | 底部固定 | 内容滚动到底/顶 | 勾选区固定在弹窗底部，不随内容滚动 | | |

## 三、回归

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| M10 | 不叠加 | 手册已开时连按 F1 / 点「？」 | 不出现第二个弹窗 | | |
| M11 | 快捷键仍生效 | 手册弹出状态下 Ctrl+↑↓ / 空格 | 音量/播控仍正常响应 | | |
| M12 | 大字号弹窗 | 「最大」档下触发启动弹出 | 手册整体等比放大、底部勾选区完整可见不裁切 | | |
| M13 | 深色主题适配 | 切「暗夜墨 · 深色」后打开手册 | 弹窗深底浅字，底部勾选框边框/勾色在深色下清晰可见，整行悬停反馈正常 | | |
| M14 | 切换生效边界 | 启动弹出后在弹窗内取消勾选 | **当次弹窗不消失不闪断**，仅影响下次启动（下次起不再弹出） | | |
| M15 | 无状态文件默认 | 关闭软件后删除 exe 同级 state.json → 重启 | 视为全新环境：手册默认弹出（manualOnStart 缺省 = true） | | |
