# EchoHymn v1.3.0 回归复测清单（2026-08-30）

> **用途**：针对 v1.3.0 新增功能第一轮测试的 **1 项 NG（H11 手册重复打开叠加）修复** + **状态栏分隔线新增需求**，进行定向回归复测。
> **版本**：`8dbf2a4` 之后的最新构建  <br>
> **测试平台**：Windows（`echo_hymn.exe`）  <br>
> **填写说明**：按「测试动作」逐步操作，核对「预期结果」；实际结果填 **OK** 或 **NG**，NG 记录具体现象（操作步骤 → 实际表现 → 与预期差异）。

---

## 一、修复项复测（H11 用户手册重复打开不叠加）

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| H11a | 打开后再次触发不叠加 | 点「？」打开手册 → 再按 `F1` → 再点「？」 | **始终只有一个手册弹窗**，不叠加新弹窗 | | |
| H11b | 关闭后可再次打开 | 手册打开 → `Esc` 关闭 → 再按 `F1` | 能再次正常弹出手册（标志已复位） | | |
| H11c | 关闭后再打开（遮罩） | 手册打开 → 点弹窗外关闭 → 再点「？」 | 能再次正常弹出手册 | | |
| H11d | 快捷键在手册内仍生效 | 手册打开时按 `空格` / `Ctrl+P` | 播放/暂停仍生效，不叠加新手册 | | |

## 二、状态栏分隔线（新增）

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| D01 | 状态栏顶部分隔线 | 查看底部状态栏上方 | 内容区与状态栏之间有 **1px 分隔线**（#E5E6EB） | | |
| D02 | 分隔线不挤占布局 | 查看窗口整体 | 状态栏高度仍为 30px；标题栏/顶栏/状态栏三条分隔线清晰，四栏无重叠 | | |
| D03 | 状态栏内容完整 | 播放一首歌 | 状态信息（编号/歌名/词曲）+ 进度（位置/时长/百分比）完整显示 | | |

## 三、相关回归（防修复破坏）

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| R01 | 手册正常打开 | 点击标题栏「？」 / 按 `F1` | 手册正常弹出；内容三部分（软件介绍/操作说明/快捷键说明）完整可滚动 | | |
| R02 | 手册正常关闭 | `Esc` / 右上角 ✕ / 点遮罩 | 手册正常关闭，无残留、无卡死 | | |
| R03 | 顶栏中央歌曲 | 播放/切歌 | 顶栏中央显示当前歌曲（第 N 首《标题》），切歌自动刷新 | | |
| R04 | 标题栏按钮 | 最小化 / 最大化·还原 / 关闭 | 功能正常；最大化图标同步（Win+↑/↓） | | |
| R05 | 全局快捷键 | 空格 / Ctrl+P / Ctrl+←/→ | 播放控制正常；输入框聚焦时空格放行输入 | | |

---

## 测试记录汇总

| 段落 | 用例数 | OK 数 | NG 数 | 备注 |
| --- | --- | --- | --- | --- |
| 一、修复项复测（H11） | 4 | | | |
| 二、状态栏分隔线 | 3 | | | |
| 三、相关回归 | 5 | | | |
| **合计** | **12** | | | |

> **提示**：H11 修复点为 `UserManualDialog` 增加 `_isOpen` 防重入标志（`whenComplete` 复位）；状态栏分隔线为 `home_screen.dart` 在内容区与状态栏之间新增 `Divider`。
