# EchoHymn v1.4.0 换肤功能修复复测清单

> **版本**：v1.4.0（2026-08-30 首轮测试 6 项 NG 修复后复测）
> **测试平台**：Windows（`echo_hymn.exe`）
> **测试方法**：人工/手工测试
> **测试准备**：① 使用最新版（`hymn_app/build/windows/x64/runner/Release/` 或 `release/` 最新目录）；② 展开左右侧栏；③ 每项如实填写「实际结果（OK/NG）」。

---

## 一、NG 修复项复测

| 编号 | 原 NG | 修复说明 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- | --- |
| R01 | T06 暗夜墨顶部白色带 | native 随主题设置 DWM 深色模式 + 边框色（`SetAppearance`/`setWindowAppearance`） | 切到「暗夜墨 · 深色」查看窗口最顶部 | 顶部无浅色/白色带，边框与标题栏同为深色（约 7px 边框区融入深色） | | |
| R02 | T14 换肤后大部分区域不变色 | `HomeScreen.build` 外包 `ValueListenableBuilder<AppPalette>` 监听调色板，整树重建 | 播放一首歌 → 点击调色盘切换到「静谧绿」 | **所有区域同步变色**：标题栏/顶栏/左栏列表/右栏/版本栏/播放条/状态栏/歌词区全部立即更新（非仅进度条） | | |
| R03 | T18 滚动条/标题栏悬停不随配色 | 同 R02（整树重建） | 换肤后在晨光蓝下滚动诗歌列表、悬停标题栏按钮，再切暗夜墨重复 | 滚动条滑块颜色、标题栏按钮悬停灰**随配色切换**；关闭按钮悬停仍红底白字 | | |
| R04 | T19/T26 暗夜墨弹窗输入框浅底浅色 | 暗夜墨改用 `ThemeData.dark()` + `ColorScheme.fromSeed(brightness: dark)` | 暗夜墨下打开「个人歌单 → 新建」弹窗，在名称/搜索输入框输入文字 | 弹窗/输入框为**深色底**，输入文字为**浅色**，清晰可读（深底浅字，非浅底浅色） | | |
| R05 | T22 实机左右栏与预览不一致 | 左栏列表行背景 `cardBg`(白)→`sidebarBg`(左栏底色) | 晨光蓝下对比实机左栏与 `docs/theme_preview.html` 预览 | 左栏整体为浅蓝 `#EBF1FA`（列表行融入背景），与预览一致 | | |
| R06 | T23 晨光蓝左栏列表内容颜色异常 | 同 R05（三个面板 + 分页栏统一） | 晨光蓝下查看诗歌列表 / 默认歌单 / 个人歌单三个栏目 | 列表行不再是大白卡片，融入左栏浅蓝背景；选中行仍高亮 + 左竖条 | | |

## 二、回归项

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果（OK/NG） | NG 状态 |
| --- | --- | --- | --- | --- | --- |
| R07 | 五套配色可切换 | 依次切 5 套配色 | 全部即时生效，暖阳金/静谧绿/典雅紫/暗夜墨/晨光蓝各自色相正确 | | |
| R08 | 换肤菜单全称 | 打开调色盘菜单 | 5 项显示全称（晨光蓝 · 经典 等）；Tooltip/日志同步 | | |
| R09 | 换肤持久化 | 切「典雅紫」→ 重启 | 重启仍为典雅紫；删 state.json 回退晨光蓝 | | |
| R10 | 换肤后播放/快捷键 | 换肤后 空格/切歌/音量/F1 | 播放与快捷键正常，音频不中断 | | |
| R11 | 深色分区层次 | 暗夜墨展开双侧栏 | 各分区相近深色层次可辨，文字清晰 | | |
| R12 | 弹窗跟随主题 | 各配色下打开用户手册 / 歌单弹窗 | 弹窗底色/文字/按钮与当前主题协调可读 | | |
| R13 | 标题栏功能 | 换肤后 拖拽/双击最大化/最小化/关闭 | 窗口操作正常 | | |
| R14 | 预览页 | 浏览器打开 `docs/theme_preview.html` | 5 套预览与实机一致（左栏/右栏/歌词区色值对齐） | | |

## 三、复测结果汇总

- 复测日期：
- 复测 NG 汇总（编号 + 现象）：
- 复测 NG 根因与修复说明（如有）：
