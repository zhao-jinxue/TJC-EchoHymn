# EchoHymn NG 修复回测清单（2026-08-28）

> **用途**：针对 2026-08-28 完成的 4 批 UI 测试 NG 修复（提交 `5ddd1f0`/`f82c503`/`c5567be`/`f37d564`）进行**定向回归**。
> **配套**：完整用例见 `ui_action_test_cases.md`；本清单只列出已修复的 23 项，回测通过后可在原清单对应行更新实际结果。
> **填写说明**：实际结果填「OK」或「NG」；NG 记录具体现象。

---

## 批 1 · 数据/底层（提交 5ddd1f0）

| 编号 | 原 NG 现象 | 修复内容 | 回测动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- |
| A01 | 首次运行显示「请在左侧选择一首诗歌」而非默认第 1 首 | 移除旧版 Roaming 数据迁移，无 state.json 时返回默认状态并加载第 1 首 | 删除 exe 同级 `state.json` 后双击启动 | 默认显示第 1 首《頌讚獨一真神》，歌词区不显示「请在左侧选择」 | |
| A03 | 关闭后 `state.json` 的 `hymnNumber` 为空 | 启动即加载默认第 1 首，关闭时写入当前歌曲 | 启动（默认第 1 首）→ 点右上角 × 关闭 → 查看 `state.json` | `hymnNumber` 为 "1"（或界面当前歌曲编号），非空 | |
| A04 | 关闭后立即再次双击，状态未正常恢复 | 移除迁移污染后恢复正常恢复链路 | 关闭后立即再次双击 | 正常启动，恢复上次歌曲，无残留进程冲突 | |
| J04 | 删除 `state.json` 后启动不是默认第 1 首，而是「默认歌单-信心-交托恩主」 | 移除迁移，删库后按全新环境处理 | 删除 `state.json` 后启动 | 默认第 1 首 + 左右栏收起，不从旧位置迁出旧状态 | |
| G11 | 切换左栏 Tab 后高亮错误（显示旧页码而非当前播放歌曲） | `restoreSaved` 改为优先按当前播放歌曲定位 | 播放某首（如第 15 首）→ 切到其他 Tab → 切回「诗歌列表」 | 高亮定位到当前播放歌曲所在页，而非旧页码 | |
| J06 | 搜索定位播放后重启，没有按编号恢复 | 恢复优先当前播放歌曲，编号兜底 | 搜索定位播放某首 → 重启 | 按编号恢复到该首，页码/滚动定位正确 | |
| K12 | 删除数据库后「默认歌单」仍显示旧内容 | 发布包目录禁止向上穿透仓库根 + 空库容错 | 删除发布包 `data/tjc_hymn.db` → 打开「默认歌单」 | 显示空态（无旧内容），不崩溃 | |
| K13 | 删除数据库后空歌单/空子目录仍显示内容 | 同上 | 删除发布包 `data/tjc_hymn.db` → 打开歌单/子目录 | 显示空态，不崩溃 | |

## 批 2 · 窗口/进程（提交 f82c503）

| 编号 | 原 NG 现象 | 修复内容 | 回测动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- |
| A02 | 再次双击启动第二个实例 | `CreateMutexW` 单实例保护 | 程序运行中再次双击 exe | 不启动新实例，聚焦已有窗口；任务管理器仅 1 个 `echo_hymn.exe` | |
| A09 | 拖拽窗口边缘可缩到小于最小尺寸 | 最小尺寸随侧栏展开同步更新 | 展开左栏后拖拽窗口边缘缩小 | 窗口最小宽度 = 基座 850 + 展开栏宽（左栏≥1200px 客户区），侧栏不被裁切 | |
| K10 | 左/右栏展开后仍可拖动缩小到基座尺寸 | 同上 | 左栏展开 → 拖动缩小窗口 | 停在含侧栏的最小尺寸，侧栏完整显示 | |

## 批 3 · 列表/搜索/播放（提交 c5567be）

| 编号 | 原 NG 现象 | 修复内容 | 回测动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- |
| C02 | 底部状态栏无播放进度 | 状态栏右侧加位置/总时长/百分比 | 播放一首歌，观察底部状态栏 | 显示「位置 / 总时长 · N%」 | |
| C03 | 搜索框 back 键删除时触发搜索定位 | 输入变化不再即时搜索，回车才搜索 | 搜索框输入编号后按 back 逐字删除 | 只删内容，列表不跳动、不触发定位 | |
| C08 | 搜索清空（×）后定位丢失，滚动回最上方 | 清空后滚回当前歌曲所在位置 | 搜索定位到某首 → 点 × 清空 | 列表停在当前歌曲所在页/位置，不回最上方 | |
| C14 | 记忆人声版后切歌遇有人声版歌不恢复人声版 | 版本 fallback 不覆盖记忆版本 | 切到人声版 → 切到无人声版歌（自动钢琴）→ 再切到有人声版歌 | 有人声版歌自动用人声版播放 | |
| K11 | 搜索一边输入一边搜索、无空态 | 回车触发搜索 + 无匹配 Toast | 搜索框输入编号/标题 | 不边输边搜；回车才定位；无匹配时弹「未找到相关诗歌」 | |

## 批 4 · 弹窗/播放条（提交 f37d564）

| 编号 | 原 NG 现象 | 修复内容 | 回测动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- | --- |
| I01 | 歌单名称输入框右下角无字数 | 恢复默认字数计数 | 打开歌单弹窗 → 名称框输入文字 | 右下角显示「N/30」 | |
| I03 | 「请输入歌单名称」Toast 显示在主界面 | 弹窗内嵌 ScaffoldMessenger | 名称留空点「创建/保存」 | Toast 显示在弹窗内，而非主界面底部 | |
| I05 | 「未找到相关诗歌」Toast 显示在主界面 | 同上 | 弹窗搜索框输入不存在内容点搜索 | Toast 显示在弹窗内 | |
| I08 | 「该诗歌已在歌单中」Toast 显示在主界面 | 同上 | 重复添加同一编号诗歌 | Toast 显示在弹窗内 | |
| I14 | 删除歌单后仍在数据库中存在 | 补 `repo.deletePlaylist` | 修改歌单弹窗 →「删除歌单」→ 确认 | 歌单从列表消失；重启后仍不存在 | |
| F03 | 👥 人声版本按钮出现时控制按钮组左移 | 播放条改 Stack 布局，版本按钮固定右侧 | 播放人声多版本歌曲，观察 👥 按钮出现 | 上一首/播放/下一首按钮位置不变 | |
| K17 | 快速连续点击「下一首」出现位移/错乱 | `playAt` 250ms 防抖 | 快速连续点击「下一首」 | 切歌稳定无错乱、无位移 | |

---

## 无需改码项（测试条件不具备，清单备注即可）

| 编号 | 说明 |
| --- | --- |
| D07 | 无空子目录（测试数据不含） |
| D08 | 无空分类数据（测试数据不含） |
| F09 | 无「暂无简谱」诗歌（测试数据不含） |
| F11 | 无「暂无五线谱」诗歌（测试数据不含） |

---

> **回测结论**：全部执行完毕后在此汇总（通过数 / 失败数 / 复测 NG 项）。
