# EchoHymn 回归测试用例清单（2026-08-28）

> **用途**：针对 UI 动作测试（`ui_action_test_cases.md`）中出现 NG 的场景，进行**回归验证**。
> **填写说明**：按「测试动作」逐步操作，核对「预期结果」是否达成；实际结果填「OK」或「NG」，NG 记录具体现象。

---

## 一、启动与状态恢复

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| A01 | 首次启动 | 删除 exe 同级 `state.json` 后双击启动 | 默认显示第 1 首《頌讚獨一真神》，歌词区不显示「请在左侧选择一首诗歌」 | |
| A03 | 关闭程序 | 启动（默认第 1 首）→ 点右上角 × 关闭 → 查看 exe 同级 `state.json` | `hymnNumber` 与界面当前歌曲一致（非空，如 "1"） | |
| A04 | 关闭后立即重启 | 关闭后立即再次双击程序 | 正常启动，恢复上次歌曲，无残留进程冲突 | |
| J04 | 状态文件缺失 | 删除 `state.json` 后启动 | 默认第 1 首 + 左右栏收起，不出现其他歌单/歌曲的旧状态 | |
| J06 | 搜索播放后重启 | 搜索定位播放某首 → 重启程序 | 按编号恢复到该首，页码/滚动定位正确 | |
| G11 | 切换左栏 Tab | 播放某首（如第 15 首）→ 切到其他 Tab → 切回「诗歌列表」 | 高亮定位到当前播放歌曲所在页，而非旧页码 | |

## 二、数据库缺失容错

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| K12 | 数据库缺失-默认歌单 | 删除发布包 `data/tjc_hymn.db` → 打开「默认歌单」 | 显示空态（无旧内容），不崩溃 | |
| K13 | 数据库缺失-歌单内容 | 删除发布包 `data/tjc_hymn.db` → 打开歌单/子目录 | 显示空态，不崩溃 | |

## 三、窗口与进程

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| A02 | 多实例启动 | 程序运行中再次双击 exe | 不启动新实例，聚焦已有窗口；任务管理器仅 1 个 `echo_hymn.exe` | |
| A09 | 拖拽调整窗口大小 | 展开左栏后拖拽窗口边缘缩小 | 窗口最小宽度 = 基座 850 + 展开栏宽（左栏≥1200px 客户区），侧栏不被裁切 | |
| K10 | 侧栏展开时缩小窗口 | 左栏展开 → 拖动缩小窗口 | 停在含侧栏的最小尺寸，侧栏完整显示 | |

## 四、搜索与列表

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| C03 | 搜索框删除键 | 搜索框输入编号后按 back 逐字删除 | 只删内容，列表不跳动、不触发定位 | |
| C08 | 搜索清除 | 搜索定位到某首 → 点 × 清空 | 列表停在当前歌曲所在页/位置，不回最上方 | |
| K11 | 搜索触发与无结果 | 搜索框输入编号/标题 | 不边输边搜；回车才定位；无匹配时弹「未找到相关诗歌」 | |

## 五、播放与版本

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| C02 | 播放进度显示 | 播放一首歌，观察底部状态栏 | 显示「位置 / 总时长 · N%」 | |
| C14 | 音频版本连播 | 切到人声版 → 切到无人声版歌（自动钢琴）→ 再切到有人声版歌 | 有人声版歌自动用人声版播放 | |
| K17 | 快速连点切歌 | 快速连续点击「下一首」 | 切歌稳定无错乱、无位移 | |
| F03 | 版本按钮布局 | 播放人声多版本歌曲，观察 👥 按钮出现 | 上一首/播放/下一首按钮位置不变 | |

## 六、歌单弹窗

| 编号 | 测试场景 | 测试动作 | 预期结果 | 实际结果 |
| --- | --- | --- | --- | --- |
| I01 | 名称字数计数 | 打开歌单弹窗 → 名称框输入文字 | 右下角显示「N/30」 | |
| I03 | 名称为空保存 | 名称留空点「创建/保存」 | Toast 显示在弹窗内，而非主界面底部 | |
| I05 | 弹窗搜索无结果 | 弹窗搜索框输入不存在内容点搜索 | Toast 显示在弹窗内 | |
| I08 | 重复添加诗歌 | 重复添加同一编号诗歌 | Toast 显示在弹窗内 | |
| I14 | 删除歌单 | 修改歌单弹窗 →「删除歌单」→ 确认 | 歌单从列表消失；重启后仍不存在 | |

---

## 测试补充说明

以下场景因当前测试数据不含对应条件，无需执行：

| 编号 | 说明 |
| --- | --- |
| D07 | 无空子目录（测试数据不含） |
| D08 | 无空分类数据（测试数据不含） |
| F09 | 无「暂无简谱」诗歌（测试数据不含） |
| F11 | 无「暂无五线谱」诗歌（测试数据不含） |

---

> **测试结论**：全部用例执行完毕后在此汇总（通过数 / 失败数 / 复测 NG 项）。

